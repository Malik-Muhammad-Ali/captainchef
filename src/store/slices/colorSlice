const colorSlice = (set) => ({
  selectedColor: null, // Store for the selected color
  setSelectedColor: (color) => {
    console.log("Selected color:", color); // Log the selected color
    set({ selectedColor: color }); // Update the selected color in the store
  },
});

export default colorSlice;
